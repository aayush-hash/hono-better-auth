{
  "name": "hono-better-auth",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up":"docker compose up -d"
  },
  "dependencies": {
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}